image:
  # Official image -- same as local, no ECR mirror needed since nothing is custom-built anymore.
  repository: pgvector/pgvector
  tag: "pg16"

environment: prod

persistence:
  size: 20Gi

resources:
  requests:
    cpu: 500m
    memory: 1Gi
  limits:
    cpu: "2"
    memory: 2Gi
